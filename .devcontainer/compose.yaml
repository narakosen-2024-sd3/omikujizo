name: omikujizo-devcontainer
services:
  app:
    build:
      context: ../app
      target: development
    privileged: true
    network_mode: host
    environment:
      - DISPLAY=$DISPLAY
    command: sleep infinity
    volumes:
      - type: bind
        source: ../app
        target: /workspace
